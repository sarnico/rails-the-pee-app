<div class="title">
  <h1>Welcome to the pee app 🚽</h1>
  <p>Nr 1 or nr 2 ? 🧻 Find the best spot around you 💩</p>
</div>

<div class="main-container">

  <div class="all-toilets">
    <% @toilets.each do |t| %>
      <%= render 'shared/toilet_card', t: t %>
    <% end %>
  </div>

  <div
    id="map"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV["MAPBOX_API_KEY"] %>"
  >
  </div>

</div>


