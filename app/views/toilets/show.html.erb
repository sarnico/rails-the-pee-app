<h1>toilet's name : <%= @toilet.name %></h1>
<h2>toilet's address : <%= @toilet.address %></h2>



ICI FORM POUR BOOKER TOILET

<%= simple_form_for [@toilet, @booking] do |f| %>
  <%= f.input :date, collection:@available_periods_today, prompt: :translate %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<div
  id="booking-settings"
  data-toilet-settings="<%= @toilet_settings.to_json %>"
  style="display:none;"
>
</div>

<div
  id='calendar'
  data-toilet-id="<%= @toilet.id.to_s %>"
  data-booking="<%= (@toilet_calendar).to_json %>"
>
</div>

